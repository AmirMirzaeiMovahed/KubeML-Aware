apiVersion: v1
kind: Pod
metadata:
  name: workload-4
  labels:
    ml.scheduler/run-id: run-heterogeneous
  annotations:
    ml.scheduler/run-id: run-heterogeneous
    ml.scheduler/estimated-training-time: "150"
    ml.scheduler/loss-reduction-rate: "0.3"
    ml.scheduler/matrix-size: "256"
    ml.scheduler/gradient-update-size: "64"
    ml.scheduler/checkpoint-interval: "25"
    ml.scheduler/model-partitions: "1"
spec:
  schedulerName: default-scheduler
  schedulingGates:
  - name: ml.scheduler/release
  containers:
  - name: train
    image: nginx
    resources:
      requests:
        memory: "128Mi"
        cpu: "200m"
      limits:
        memory: "256Mi"
        cpu: "500m"